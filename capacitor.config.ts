import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.bridge',
  appName: 'ionic-bridge',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
